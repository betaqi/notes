##  阶段 一：AI 模型生态与推理基础设施
本阶段核心：理解 AI 工程底层生态，掌握模型获取、下载、推理与部署体系。
### 0-1 Hugging Face 与 ModelScope 双生态理解【必学】
理解：
- Hugging Face Hub 仓库结构
- model / dataset / space 三类资源
- tokenizer.json / config.json / generation_config.json 作用
- pytorch_model.bin 与 safetensors 区别
- checkpoint shard 分片机制
- model revision / branch / tag
- LFS 大文件存储
- inference endpoint 基础概念
- model card 编写规范
- dataset split（train/valid/test）
- space demo 部署原理
- GGUF / AWQ / GPTQ 模型格式
- PEFT / LoRA 模型结构
重点：
- HuggingFace CLI 使用
- 国内镜像源配置
- Git 下载与 snapshot 下载区别
- 大模型目录结构识别
- 模型许可证（Apache2.0 / MIT / Qwen License）
- 开源模型商用限制
---
### 0-2 ModelScope 模型下载与缓存机制【必学】
补充：
- snapshot_download 参数详解
- cache_dir 自定义缓存目录
- revision 指定版本
- local_dir 本地落盘
- symlink 软链接机制
- 多模型共享缓存
- 模型断点续传
- 权重 hash 校验
- 离线推理目录组织
- 模型热更新机制
- Linux 下模型存储规划
理解：
- 模型缓存目录
- 权重结构
- 本地模型路径
- 离线部署
- ~/.cache 目录结构
- tokenizer 与 model 权重分离
- embedding 模型目录结构
- rerank 模型目录结构
- 多卡权重切分

### 0-3 Transformers 基础加载【必学】
学习：

- AutoModel
- AutoTokenizer
- pipeline
- text-generation
- embeddings
- AutoModelForCausalLM
- AutoModelForSequenceClassification
- AutoModelForEmbedding
- tokenizer.apply_chat_template
- device_map="auto"
- torch_dtype
- trust_remote_code
- batch inference
- generate 参数控制
- streamer 流式输出
理解：
- tokenizer
- context length
- generation config
- KV Cache 原理
- Prefill / Decode 阶段
- EOS Token
- temperature / top_p / top_k
- repetition penalty
- max_new_tokens
- prompt template
- system prompt 注入
- chat template 差异
### 0-4 Ollama / Transformers / vLLM 推理体系区别【必学】
理解：

| 系统           | 定位          |
| ------------ | ----------- |
| Ollama       | 本地轻量推理      |
| Transformers | Python 原生推理 |
| vLLM         | 高性能生产级推理    |

补充：
- Continuous Batching
- Paged Attention
- KV Cache 管理
- tensor parallel
- pipeline parallel
- speculative decoding
- GPU utilization
- token latency
- request queue
- async engine

重点：

- GPU 显存
- 并发
- Token 吞吐
- Streaming
- Ollama Modelfile
- vLLM OpenAI API 兼容层
- Transformers 单请求推理
- vLLM 高并发优势
- 显存占用估算
- 多模型部署策略
- CPU 推理 vs GPU 推理

### 0-5 国内开源模型生态认知【选学】

认识：

- Qwen
- DeepSeek
- GLM
- InternLM
- MiniCPM
- InternVL

理解各模型定位。


## 阶段二：大模型原生 API 激活与 FastAPI 极速入门

本阶段核心： 打通 Python 后端大模型原生流式调用的最小闭环，掌握大语言模型的底盘通信能力。

### 1-1 大模型原生 SDK 接入与异步客户端初始化 (OpenAI/Anthropic) [必学]
补充：

- AsyncOpenAI
- Anthropic Async Client
- API Base 配置
- timeout 设置
- proxy 配置
- max_retries
- connection pool
- asyncio 基础
- await / async
- 并发请求 gather
- streaming chat completion

### 1-2 提示词工程基础与 System/User/Assistant 消息角色管理 [必学]

补充：

- Few-shot Prompt
- Chain of Thought
- ReAct Prompt
- XML Prompt
- JSON Prompt
- Prompt Injection
- Jailbreak
- Prompt Guard
- system prompt 分层设计
- memory prompt
- role prompt

理解：

- Prompt 模板化
- 动态变量注入
- prompt 压缩
- prompt 缓存

### 1-3 结构化输出 (Structured Output) 与 Pydantic v2 数据校验 [必学] 🔥AI时代核心

补充：

- BaseModel
- Field
- model_validate
- model_dump
- Literal
- Enum
- nested schema
- schema_json
- response_format
- json schema
- tool schema

理解：

- 结构化输出稳定性
- hallucination 修复
- schema retry
- strict mode

### 1-4 Token 消耗原理、计算工具 (Tiktoken) 与上下文剪裁策略 [必学]

补充：

- tokenizer encode/decode
- token 统计
- context overflow
- sliding window
- summarize memory
- truncation strategy
- conversation compression
- history clipping

理解：

- 中文 token 特点
- prompt 成本控制
- 长上下文性能衰减

### 1-5 Python 异步生成器与流式传输 (Streaming Delta) 原生解析 [必学]

补充：

- yield
- async generator
- async for
- chunk parser
- delta.content
- tool_calls streaming
- incremental rendering
- backpressure
- websocket vs SSE

理解：

- 流式响应生命周期
- chunk 拼接
- markdown 流式渲染

### 1-6 FastAPI 异步路由体系、请求体解析与 CORS 跨域配置 [必学] ⚡前端快速跳过

补充：

- APIRouter
- Depends
- Request
- Response
- Middleware
- CORSMiddleware
- 生命周期 hooks
- uvicorn
- gunicorn
- async endpoint
- pydantic request model


### 1-7 基于 StreamingResponse 的标准 SSE (Server-Sent Events) 接口封装 [必学]
补充：

- text/event-stream
- event id
- retry 字段
- heartbeat
- stream close
- reconnect
- incremental markdown
- stream error handling

理解：

- SSE 与 websocket 区别
- 浏览器 EventSource

📐 阶段实战项目： 《基于 Vue3 + FastAPI 的高并发流式 AI 聊天面板》

要点：自研 Python 后端大模型网关，前端完美对接 SSE 流，支持流式 Markdown 与打字机特效。

## 阶段三：工具调用、MCP 协议与 Cursor 智能工程流

本阶段核心： 让大模型长出“手和脚”，学会通过 API 操控本地系统与外部服务。前端在此阶段重点攻克“数据卡片可视化交互”。

### 2-1 Function Calling 原理
补充：

- tool_choice
- parallel tool calls
- strict schema
- JSON Schema types
- required fields
- enum
- nested object
- tool registry
- dynamic tools

理解：

- 模型如何选择工具
- tool hallucination
- schema 设计技巧

### 2-2 闭环工具执行流：模型意图捕获、本地函数动态调用与结果二次投喂 [必学]

补充：

- tool dispatcher
- function router
- dynamic invoke
- tool result injection
- observation message
- retry mechanism
- async tool execution
- tool timeout

理解：

- Agent 执行循环
- reasoning → acting

### 2-3 工具异常捕获与自我纠错  (Self-Correction) 机制 [必学]

补充：

- try/except
- tool fallback
- invalid args 修复
- retry prompt
- tool validation
- self-healing
- guardrail

理解：

- 模型错误恢复机制
- 错误分类

### 2-4 MCP (Model Context Protocol) 协议核心架构 (Client-Server-Tools) 深度解析 [必学] 🔥2026全新核心
补充：

- MCP transport
- stdio transport
- websocket transport
- MCP resources
- MCP prompts
- MCP tools
- MCP lifecycle
- capability negotiation

理解：

- MCP Client
- MCP Server
- Tool Discovery
- Context Sync


### 2-5 使用 FastMCP Python 库快速构建自定义 MCP Server [必学]
补充：

- tool decorator
- resource decorator
- async tool
- local file access
- subprocess tool
- MCP logging
- server startup

理解：

- MCP 服务暴露机制
- Cursor 工具发现

### 2-6 MCP 联动高级工程流：配置 Cursor / Claude Code 调用本地自定义工具 [选学]
补充：

- Cursor Rules
- Claude Code Hooks
- 本地文件系统访问
- Git 操作自动化
- terminal tool
- code generation workflow

### 2-7 前端交互演进：在 SSE 流中拦截 tool_calls 并实现动态工具卡片 (Tool Cards) 渲染 [必学]
补充：

- tool status state
- pending/running/success/error
- SSE tool event
- 动态 JSON 渲染
- markdown + card 混合流
- tool timeline

📐 阶段实战项目： 《基于 MCP 协议的本地代码库资产审计与全自动可视化助手》

要点：编写 Python MCP 操控本地 Git 与文件，前端拦截工具状态并用图形化卡片展示执行过程。

## 阶段四：企业级 RAG (检索增强生成) 架构与深度优化

本阶段核心： 解决大模型幻觉，注入企业私有数据资产。此阶段完全抛弃浅层前端玩具 RAG，直击企业级高准确率检索痛点。

### 3-1 RAG 核心三阶段原理：数据准备、检索、生成 [必学]
补充：

- ingestion pipeline
- retrieval pipeline
- generation pipeline
- grounding
- hallucination
- citation
- chunk recall
### 3-2 数据清洗策略与智能文档切片方案 (RecursiveCharacter / 语义切片) [必学]

补充：

- RecursiveCharacterTextSplitter
- semantic chunking
- overlap
- metadata tagging
- title enhancement
- OCR 文本清洗
- markdown parsing
- PDF extraction

理解：

- chunk 大小控制
- 召回率影响

### 3-3 向量化 (Embeddings) 模型调用与向量数据库 (Chroma / Qdrant) 索引构建 [必学]
补充：

- Chroma collection
- Qdrant collection
- vector dimension
- HNSW 索引
- metadata filter
- payload
- batch insert
- persistence

理解：

- ANN 检索
- vector distance

### 3-4 混合检索 (Hybrid Search) 技术：密集向量检索 + 传统 BM25 关键词检索 [必学] 🔥AI时代核心
补充：

- BM25
- inverted index
- score fusion
- weighted retrieval
- reciprocal rank fusion
- keyword boosting

理解：

- 关键词召回
- 语义召回融合

### 3-5 Rerank 重排序模型 (FlashRank / CrossEncoder) 应用与精筛策略 [必学]
补充：

- CrossEncoder rerank
- FlashRank
- top-k refine
- retrieval compression
- semantic filtering

理解：

- rerank 对准确率提升
- latency 成本

### 3-6 语义路由 (Semantic Routing) 机制：闲聊/知识库请求的动态分流 [必学]
补充：

- intent classification
- route chain
- query classifier
- fallback route
- multi knowledge base routing

### 3-7 LlamaIndex 与 LangChain RAG 高级组件对比与接入技巧 [选学]
补充：

- retriever abstraction
- node parser
- query engine
- retriever chain
- document store
- callback manager

### 3-8 RAG 质量评估体系 (Ragas 框架：检索相关性与生成忠实度度量) [选学]
补充：

- faithfulness
- answer relevancy
- context precision
- context recall
- synthetic dataset
- evaluation pipeline

### 3-9 GraphRAG (知识图谱增强) 与 Multimodal RAG (多模态知识库) [以后再学]
补充：

- entity extraction
- relation graph
- graph traversal
- image embedding
- vision-language model
- OCR pipeline

### 3-10 RAG 服务的 FastAPI 封装与端侧流式溯源 (Citations / 引用光源) 输出 [必学]
补充：

- citation output
- source tracing
- streaming answer
- retriever API
- async retrieval
- frontend citation highlight

📐 阶段实战项目： 《企业级私有海量多格式文档（PDF/MD）智能混合检索问答平台》

要点：后端实现“混合检索 + Rerank”，前端交互实现点击引用光源精准高亮定位原文。

## 阶段五：高级 Agent 编排与 LangGraph 状态机工作流

本阶段核心： 从简单的单向 Chain 进化为具备“循环、反思、记忆、自主决策”的复杂 Agent。LangGraph 是 2026 年行业标准的图结构流控制工具。

### 4-1 LangChain 统一模型接入口与 LCEL (表达式语言) 管道语法 [选学]
补充：

- Runnable
- RunnableLambda
- RunnableParallel
- pipe operator
- output parser
- prompt template chain

### 4-2 LangGraph 核心架构：节点 (Nodes)、图编译与条件路由 (Conditional Edges) [必学] 🔥AI时代核心
补充：

- StateGraph
- START / END
- add_node
- add_edge
- conditional_edge
- graph compile
- router node

### 4-3 全局状态管理 (State Management) 与上下文在图节点中的传递 [必学] ⚡前端快速跳过 (类 Pinia 概念)

补充：

- TypedDict State
- shared state
- reducer
- message history
- state merge
- immutable update

### 4-4 经典 ReAct (Reasoning and Acting) 闭环 Agent 模式构建 [必学]

补充：

- Thought
- Action
- Observation
- reasoning loop
- iterative planning

### 4-5 LangGraph 持久层与 Checkpoints 机制（会话线程 Thread 管理） [必学]

补充：

- MemorySaver
- SqliteSaver
- thread_id
- conversation resume
- persistent state

### 4-6 时间旅行 (Time Travel)：Agent 历史状态回滚、修改与分支测试 [必学]
补充：

- rollback
- replay
- branch testing
- checkpoint restore

### 4-7 人工介入 (Human-in-the-loop)：工作流挂起、敏感操作中断与前端审批流对接 [必学] 🔥AI时代核心
补充：

- interrupt
- approval workflow
- pause/resume
- human feedback
- sensitive action confirmation

### 4-8 多 Agent 协作编排：Supervisor (主管模式)、子图 (Sub-graphs) 与 Map-Reduce 模式 [选学]
补充：

- supervisor agent
- worker agent
- subgraph orchestration
- task decomposition
- map reduce workflow

### 4-9 状态机自我纠错、反思机制 (Self-Reflection) 与死循环熔断处理 [必学]
补充：

- reflection node
- critique loop
- retry planning
- infinite loop protection
- max iteration

### 4-10 LangSmith 全链路 Trace 追踪、性能瓶颈分析与 Prompt 在线版本管理 [必学]
补充：

- trace visualization
- token metrics
- latency metrics
- prompt versioning
- run evaluation
- production debugging

📐 阶段实战项目： 《带人工审批机制（Human-in-the-loop）的全自动多 Agent 软件需求分析与工程生成器》

要点：构建包含“产品经理 Agent -> 架构师 Agent -> 程序员 Agent”的 LangGraph 复杂流，前端可视化展示图节点的实时流转状态与人工审批弹窗。
%%
阶段四有点偏高级，可以拆成两轮
第一轮只学：
- Node
- State
- Edge / Conditional Edges
- Tool Node
- Checkpoint
- Human-in-the-loop
- Streaming
- Error Retry
第二轮再学：
- Time Travel
- Subgraph
- Map-Reduce
- Supervisor
- Self-Reflection
- 多 Agent 协作 %%

## 阶段六：商业化落地、AI 工程运维与全栈部署

本阶段核心： 将你的 AI 应用脱离本地 localhost，具备抗压、弹性、容错的线上商业化服务能力。

### 5-1 场景化大模型上下文设计（AI 编程助手 Rule 体系、AI 导购多级记忆体系） [选学]
补充：

- 长期记忆
- 短期记忆
- 用户画像
- AI Rules
- memory compression
- context hierarchy

### 5-2 大模型 API 超时、限流 (Rate Limit) 截获与 Tenacity 指数退避重试机制 [必学]

补充：

- rate limit
- exponential backoff
- retry jitter
- circuit breaker
- request queue
- fallback model

### 5-3 生产环境大模型网关安全防御、流式断线重连 (Reconnection) 容错处理 [必学]

补充：

- API key protection
- streaming reconnect
- auth middleware
- request validation
- anti prompt injection
- websocket fallback

### 5-4 Python Poetry 依赖管理与 Dockerfile 多阶段分层轻量化构建 [必学]

补充：

- pyproject.toml
- lock file
- multi-stage build
- slim image
- dependency cache
- docker layer

### 5-5 Docker-compose 生产环境多容器（FastAPI + Qdrant + Redis）一键编排部署 [必学]

补充：

- service network
- volume persistence
- healthcheck
- restart policy
- redis cache
- qdrant persistence

### 5-6 云服务部署（Railway/Render快速上线）[必学]
补充：

- Railway deployment
- Render deployment
- docker deploy
- domain binding
- HTTPS
- auto restart

### 5-7 环境变量与密钥管理（.env + 云端Secrets）[必学]  
补充：

- dotenv
- secrets manager
- runtime env
- API key rotation
- production config split

### 5-8 基础监控与日志（结构化日志 + 错误告警）[选学]
补充：

- structured logging
- request id
- error tracking
- metrics dashboard
- OpenTelemetry
- Grafana
- Sentry

📐 阶段实战项目： 《商用级别一键部署的智能客服协同平台 (含知识库+复杂退换货状态机工作流)》


