
> 面向零基础的渐进式教程。从"模型是什么"一路讲到"在自己电脑上跑起来"。
> 阅读建议:按章节顺序读,遇到不懂的术语先跳过,最后看附录的术语表。
>
> **本指南状态**:2026 年 6 月更新。AI 领域半年一个时代,本文按"现在大家都这么用"的视角写。
> 一些 2024 年还重要、2026 年已经被替代或弱化的技术(纯文本 ChatGLM、ChatTTS 等老一代),只在历史背景里提一下。
> 现在的主线是:**推理模型已成默认 / Agent 与 MCP 标配 / 多模态原生 / 1M 上下文常态化 / 端侧大模型崛起**。

---

## 目录

1. 第 1 章 · 什么是 AI 模型(从直觉开始)
2. 第 2 章 · 模型是怎么"造"出来的:训练流程
3. 第 3 章 · 模型家族图谱:语言、多模态、专用模型
4. 第 4 章 · 看懂模型名字:后缀和标签全解
5. 第 5 章 · 量化是什么:为什么 GGUF / AWQ / FP8 这么多
6. 第 6 章 · 推理工具生态:Ollama / LM Studio / vLLM 怎么选
7. 第 7 章 · 实战一:在自己电脑上跑第一个模型
8. 第 8 章 · 实战二:用 API 接入应用
9. 第 9 章 · 进阶玩法:RAG、微调、Agent
10. 第 10 章 · 学习路径与资源
11. 附录 · 术语表

---

## 第 1 章 · 什么是 AI 模型(从直觉开始)

### 1.1 一句话比喻

可以把"模型"想成一个**超级复杂的函数**。你给它输入(文字、图片、声音),它给你输出(文字、图片、声音)。

```
输入 → [ 模型 ] → 输出
"今天天气真" → [ 语言模型 ] → "好"
```

这个函数里面有几亿到几千亿个"旋钮"(叫**参数**,英文 parameter)。这些旋钮的具体数值,就决定了模型的能力。

### 1.2 参数和模型大小

参数量常见单位:
- M = Million(百万)
- B = Billion(十亿)
- T = Trillion(万亿,前沿模型已经常见)

例子(2026 年常见档位):
- Qwen3-0.6B / Gemma3-1B = 几亿参数,**手机能跑**(端侧模型已普及)
- Llama 4-8B / Qwen3-8B = 80 亿参数,普通游戏显卡能跑
- Qwen3-30B-A3B、GLM-4.6 等 MoE = 几百亿总参,激活几十亿,消费级硬件可跑
- DeepSeek-V3.2 / Qwen3-235B-A22B / Kimi K2 = 旗舰级 MoE,几千亿参数
- 商业前沿(GPT-5、Claude 4、Gemini 3)= 不公开,推测 1T+ 总参数

**经验公式**:用 FP16(半精度)存,显存需求 ≈ 参数量 × 2 GB。
- 8B 模型 ≈ 16 GB 显存
- 70B 模型 ≈ 140 GB 显存

后面讲的"量化"就是把这个数字打折。**2026 年的现实是:经过 4-bit 量化,30B 级别的 MoE 模型在 16GB 显存上已经能跑得很流畅,这是 2024 年没法想象的。**

### 1.3 模型 ≠ 应用

很多人把 ChatGPT 和 GPT 混为一谈。其实:
- **GPT-5** 是模型(那个函数本身)
- **ChatGPT** 是应用(网页 + 模型 + 系统提示词 + 工具调用 + 记忆)

2026 年的应用还普遍套了 Agent 框架(自动调用工具、上网搜索、写代码、操作浏览器)。所以你看到的"ChatGPT 这么强"里,有相当一部分是工具和编排带来的,而不是单纯模型能力。

你下载到本地的,只是模型权重文件。要让它"会聊天",还需要一个推理程序(Ollama / LM Studio 等)给它套个壳。要让它"会干活",还要再套一层 Agent 框架。

### 1.4 模型权重文件长什么样

打开 Hugging Face 上一个模型仓库,常见文件:

```
config.json              ← 模型结构说明
tokenizer.json           ← 分词器(把文字切成 token)
model-00001-of-00004.safetensors  ← 权重(分片存储)
model-00002-of-00004.safetensors
...
```

`.safetensors` 是新一代安全格式,`.bin` / `.pt` 是旧的 PyTorch 格式。`.gguf` 是 llama.cpp 用的单文件格式,后面会讲。

---

## 第 2 章 · 模型是怎么"造"出来的:训练流程

理解训练流程,才能看懂为什么会有 Base、Instruct、Chat、DPO、Thinking 这些版本。

### 2.1 现代四阶段(2026 版)

2024 年以前训练流程是三阶段(预训练 → SFT → RLHF)。
2025 年起,**推理后训练(Reasoning RL)成为标配**——这就是为什么现在新模型几乎都带"思考模式"。

```
阶段一:预训练 (Pretraining)
  数据:互联网 + 代码 + 数学 + 多模态(2026 年通常是混合模态预训练)
  目标:学会"预测下一个 token"
  产物:Base 模型(只会续写)
  耗时:几个月,数千到数万张 H100/H200/B200,成本千万到上亿美元

      ↓

阶段二:监督微调 (SFT)
  数据:高质量"问答对",几十万到几百万条,2026 年大量是 AI 合成
  目标:学会按指令回答
  产物:Instruct 模型 / SFT 模型

      ↓

阶段三:偏好对齐 (DPO / RLHF)
  数据:人类或 AI 对回答的排序
  目标:更有用、更无害、更有礼貌
  产物:Chat / DPO 模型,可日常对话

      ↓

阶段四:推理后训练 (Reasoning RL,2025 年起成主流)
  方法:RLVR(可验证奖励强化学习)、DeepSeek R1 提出的 GRPO 等
  数据:数学、代码、逻辑题(有标准答案,机器自己判分)
  目标:让模型学会长链思考,先想再答
  产物:Thinking / Reasoning 模型
```

**2026 年的关键词:Hybrid Thinking(混合思考)**
最新一代模型(GPT-5、Claude 4、Qwen3、DeepSeek V3.2)开始把"普通模式"和"思考模式"合二为一,模型自己决定要不要思考、思考多久。用户不用再在两个模型之间切换。

### 2.2 类比:培养一个学生

- **预训练** = 让他从小博览群书,知识量爆炸,但不知道怎么"做题"
- **SFT** = 老师教他做题套路,看了一堆例题,会按规矩答题了
- **RLHF/DPO** = 通过模拟考试和反馈,让他答得更让人满意
- **推理后训练** = 让他学会"先打草稿、再写答案",做难题不再凭直觉

所以你看到的版本后缀对应:
- `Base` / `Pretrained` → 阶段一产物,不会聊天,适合二次开发
- `SFT` / `Instruct` / `IT` → 阶段二产物,能听话
- `Chat` / `RLHF` / `DPO` → 阶段三产物,日常使用首选
- `Thinking` / `Reasoning` / `R1` / `o-style` → 阶段四产物,适合数学/代码/复杂推理

### 2.3 [[4.2026 大模型前沿技术速通指南]]

**蒸馏 (Distill)**
让小模型模仿大模型的输出。例如 `DeepSeek-R1-Distill-Qwen-7B` 把 R1 推理能力蒸馏到 Qwen-7B。2026 年蒸馏成了"小模型逆袭"的主要手段——不少 7B 模型靠蒸馏旗舰模型才达到当年 70B 水平。

**合成数据 (Synthetic Data)**
用大模型生成训练数据再喂给小模型。2026 年大多数 SFT 数据都是合成的,真人标注主要用在最后一步对齐和评估上。

**RLVR (Reinforcement Learning with Verifiable Rewards)**
强化学习用"可验证答案"当奖励信号——数学题对错、代码能不能跑、单元测试是否通过。不需要训练奖励模型,直接用环境判分。这是 R1 / o3 这类推理模型变强的核心方法。

**GRPO**
DeepSeek 提出的 RL 算法,显存友好,2025 年起成为开源社区主流。

**LoRA / QLoRA / Adapter**
不是完整模型,而是"补丁"权重。需要配合 base 模型加载。体积小(几十 MB ~ 几 GB)。详见 §9.2.1。

**MoE (Mixture of Experts,混合专家)**
2026 年的旗舰开源模型几乎全是 MoE 架构。例如 `Qwen3-235B-A22B`、`DeepSeek-V3-671B-A37B`、`Kimi K2`。优点:推理省算力。缺点:显存仍要装下完整模型。

**推理模型 (Reasoning / Thinking Model)**
回答前先输出一长串思考(Chain-of-Thought),再给最终答案。代表:OpenAI o3、Claude 4 Thinking、DeepSeek-R1、Qwen3-Thinking、QwQ-Max、GLM Z1。

**思考预算 (Thinking Budget)**
2026 年新增参数,控制模型"想多久"。简单题想 1000 token,难题想 10 万 token。Claude、Gemini、Qwen3 都支持。

---

## 第 3 章 · 模型家族图谱

### 3.1 按"能干什么"分类

```
                       AI 模型
                         │
       ┌─────────────────┼─────────────────┐
       │                 │                 │
   语言模型           多模态模型          专用模型
   (LLM)             (VLM/Omni)        (Embedding 等)
       │                 │                 │
   ChatGPT             GPT-4o          Embedding
   Claude              Gemini          Reranker
   Llama               Qwen-VL         代码模型
   Qwen                InternVL        数学模型
   DeepSeek            CosyVoice       图像生成
                                        视频生成
```

### 3.2 语言模型(LLM)主流家族(2026 年视角)

**闭源(API 调用)**
- **GPT 系列**(OpenAI):GPT-5、o3 / o4 推理模型
- **Claude 系列**(Anthropic):Claude 4 Opus / Sonnet / Haiku,带 Hybrid Thinking
- **Gemini 系列**(Google):Gemini 3 Pro / Flash,原生 1M~10M 上下文
- **国内闭源**:豆包(字节)、Kimi K2(月之暗面)、Doubao 1.5、文心 5、通义闭源版

**开源(权重可下载)**
- **Llama 4 系列**(Meta):2025 年发布的原生多模态 + MoE
- **Qwen 系列**(阿里):**目前开源界综合最强之一**,Qwen3(密集) / Qwen3-MoE / Qwen3-Coder / Qwen3-Omni
- **DeepSeek 系列**:V3.2 / R1.x 系列,性价比标杆,数学和代码顶级
- **GLM 系列**(智谱):GLM-4.6、GLM Z1 推理版
- **Mistral 系列**(法国):Mistral Large 3、Mixtral 系列
- **Gemma 3 系列**(Google):多模态轻量级,端侧友好
- **Phi 4 / Phi 5**(微软):小模型典范,数学强
- **MiniMax M 系列**:超长上下文(支持百万级)
- **Kimi K2 开源版**:超长上下文方向
- **其他活跃**:Yi、Baichuan、零一万物、阶跃 Step

> 历史背景:2024 年的 Llama 3、Qwen 2.5、GPT-4 等仍然能跑,但已不是首选——同级别的 2026 模型在推理、长上下文和多模态上有代际差距。

### 3.3 多模态模型

2026 年的趋势:**原生多模态(Native Multimodal)成为旗舰标配**——文本、图像、音频、视频在预训练阶段就融合在一起,而不是后期外挂"视觉编码器"。

按输入输出模态分:

| 类型          | 输入       | 输出   | 2026 年代表                                                                                                                                                                                                                                           |
| ----------- | -------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LLM（大语言模型）  | 文本       | 文本   | GPT-5、Qwen3、DeepSeek-R1、Gemini 3                                                                                                                                                                                                                   |
| VLM（视觉语言模型） | 文+图+视频   | 文    | Qwen3-VL、InternVL3.5、Gemini 3 Vision、Keye-VL 2.0 ([arXiv](https://arxiv.org/abs/2511.21631?utm_source=chatgpt.com "Qwen3-VL Technical Report"))                                                                                                    |
| 文档理解        | PDF/截图   | 结构化文 | Qwen-VL、GPT-5、Pixtral                                                                                                                                                                                                                              |
| 图像生成模型      | 文/图      | 图    | FLUX.1、GPT Image、Midjourney V8、Seedream 4.0                                                                                                                                                                                                        |
| 图像编辑        | 图+文      | 图    | FLUX Kontext、GPT Image、Qwen-Image-Edit                                                                                                                                                                                                             |
| 视频生成模型      | 文/图/视频   | 视频   | Seedance 2.0、Veo 3.1、Kling 3.0、Runway Gen-4.5、Sora 2 ([Art & Algorithms](https://www.artandalgorithms.ai/articles/creative/ai-video-models-2026?utm_source=chatgpt.com "AI Video Models in 2026: Seedance 2.0 Takes the Lead — Art & Algorithms")) |
| 视频理解        | 视频       | 文    | Qwen3-VL、Gemini 3、GPT-5                                                                                                                                                                                                                            |
| ASR（语音识别）   | 音频       | 文本   | Whisper、SenseVoice、FunASR                                                                                                                                                                                                                          |
| TTS（语音合成）   | 文本       | 音频   | CosyVoice 3、ChatTTS、ElevenLabs                                                                                                                                                                                                                     |
| 实时语音        | 音        | 音    | GPT-5 Realtime、Gemini Live、Doubao Realtime                                                                                                                                                                                                         |
| Omni（全模态模型） | 文/图/音/视频 | 任意模态 | GPT-5 Omni、Qwen3.5-Omni、Gemini 3 ([Reddit](https://www.reddit.com/r/singularity/comments/1s7rcwr/qwen35_omni_qwens_latest_generation_of_fully/?utm_source=chatgpt.com "Qwen3.5 Omni - Qwen’s latest generation of fully omnimodal LLM"))           |
| 端到端音乐       | 文        | 音乐   | Suno v5、Udio、可灵音乐                                                                                                                                                                                                                                  |



### 3.4 专用模型

**Embedding 模型**
把文字变成向量,用于语义搜索和 RAG。
2026 年代表:`bge-m3` 系列、`Qwen3-Embedding`、`text-embedding-4`、`gte-Qwen2`、`Jina v3`、`Voyage 3`。
趋势:多模态 embedding 兴起(图文混合检索)、长文档 embedding(8K~32K)。

**Reranker(重排序)模型**
代表:`bge-reranker-v2-m3`、`Qwen3-Reranker`、`Cohere rerank-3`、`Jina Reranker`。

**代码模型**
代表:`Qwen3-Coder`(开源界最强之一)、`DeepSeek-Coder V2`、`Codestral`、`Llama 4 Code`。
2026 年代码模型已和 Agent 深度结合,通常配合 Cursor / Claude Code / Cline 使用。

**数学模型**
专用数学模型逐渐被通用推理模型(R1、o3、Qwen3-Thinking)取代。剩下少数顶配如 `DeepSeekMath V2`、`NuminaMath`、`Qwen3-Math`。

**科学模型**
- AlphaFold 3:蛋白质 + DNA + RNA + 小分子复合结构
- ESM 3 / Evo 2:生物序列大模型
- GraphCast / GenCast:天气预报
- ChemLLM、MatLLM:化学和材料

**端侧模型(2026 年的新主角)**
专为手机、笔电、边缘设备设计:
- **苹果 Intelligence**:3B 端侧 + 服务器混合
- **Gemma 3-1B/4B**、**Qwen3-0.6B/1.7B**、**Phi 4-mini**
- **MNN / MLC LLM / executorch / llama.cpp** 是端侧推理引擎

---

## 第 4 章 · 看懂模型名字

模型名其实是一串"标签",拆开看就懂。

### 4.0 先理清:四个独立维度(超级重要)

很多人把 `Instruct` 和 `GGUF` 当成同一类东西,其实它们属于**完全不同的维度**。读模型名要分四层来看:

```
   ┌──────────────────────────────────────────────┐
   │  维度 1:系列 + 规模    它是谁              │
   │  例:Qwen2.5-7B、Llama-3.1-70B              │
   ├──────────────────────────────────────────────┤
   │  维度 2:训练阶段       它会不会聊天        │
   │  例:Base / Instruct / Chat / DPO           │
   ├──────────────────────────────────────────────┤
   │  维度 3:格式           用哪个推理引擎加载  │
   │  例:原始(safetensors)/ GGUF / AWQ / MLX  │
   ├──────────────────────────────────────────────┤
   │  维度 4:量化精度       体积和质量平衡      │
   │  例:FP16 / FP8 / Q4_K_M / Q8_0             │
   └──────────────────────────────────────────────┘
```

**关键认知**:同一个模型可以同时存在多种格式版本,内容完全一样,只是打包方式不同:

```
Qwen2.5-7B-Instruct                 ← 原版(safetensors,Transformers 加载)
Qwen2.5-7B-Instruct-GGUF            ← GGUF 格式(Ollama / llama.cpp 用)
Qwen2.5-7B-Instruct-AWQ             ← AWQ 量化(vLLM 用)
Qwen2.5-7B-Instruct-GPTQ-Int4       ← GPTQ 4 位量化
Qwen2.5-7B-Instruct-MLX             ← Mac 专用格式
```

它们的"脑子"是同一个(都是 Qwen2.5-7B 的 Instruct 阶段产物),只是文件打包方式不同,适配不同的推理引擎。

**类比**:
- 维度 2(训练阶段)= 这部电影的内容(动作片 / 喜剧 / 纪录片)
- 维度 3(格式)= 它是 MP4 还是 MKV 还是 BluRay
- 维度 4(量化)= 是 4K 原盘还是 1080p 压缩版

**所以常见问法的正确回答**:

> 「GGUF 是哪个训练阶段?」
> → 错位了。GGUF 是格式,跟训练阶段平行。`Base-GGUF` 和 `Instruct-GGUF` 都合法,前者仍然不会聊天。

> 「我下载哪个版本?」
> → 先按维度 2 选 Instruct/Chat,再按维度 3+4 选适合你硬件的格式量化组合。

下面进入具体例子。

### 4.1 一个完整例子

`Qwen3-Coder-Next-30B-A3B-Instruct-FP8`

拆开:
- `Qwen3` → 系列名(通义千问第 3 代)
- `Coder` → 能力标签(代码专用)
- `Next` → 版本标签(下一代,可能是预览版)
- `30B` → 参数量(300 亿)
- `A3B` → MoE 激活参数(每次推理用 30 亿)
- `Instruct` → 训练阶段(已做指令微调,能聊天)
- `FP8` → 量化格式(8 位浮点)

### 4.2 标签清单速查

**系列 / 版本**
| 标签 | 含义 |
|------|------|
| V2 / V3 / 3.1 | 第几代 |
| Next / Preview / Beta | 预览版,可能不稳定 |
| RC | Release Candidate,接近正式版 |
| Turbo / Flash / Lite / Mini / Nano | 同代里更小、更快、更便宜的子版本(GPT-4-Turbo、Gemini-Flash) |
| Pro / Ultra / Max / Opus | 同代里更大、更强的旗舰版(Claude Opus、Gemini Ultra) |

**规模**
| 标签 | 含义 |
|------|------|
| 0.5B / 7B / 70B | 总参数量(B=十亿) |
| Mini / Small / Medium / Large | 规模档位 |
| A3B / A22B | MoE 模型的激活参数 |

**训练阶段**
| 标签 | 含义 | 能直接对话吗 |
|------|------|----------|
| Base / Pretrained | 仅预训练 | 不能,只会续写 |
| SFT | 监督微调 | 能,但可能粗糙 |
| Instruct / IT | 指令微调 | 能,日常使用首选 |
| Chat | 对话优化 | 能 |
| DPO / RLHF / ORPO | 偏好对齐 | 能,通常更好 |
| Thinking / Reasoning / R1-style | 推理后训练,带思维链 | 能,推理任务首选 |
| Distill | 蒸馏自更大模型 | 看具体 |

**能力 / 模态**
| 标签 | 含义 |
|------|------|
| VL / Vision | 看图(视觉语言) |
| Audio / ASR / TTS | 音频处理 |
| Omni / AnyToAny | 全模态 |
| Coder / Code | 代码专用 |
| Math | 数学专用 |
| Embedding | 向量化 |
| Reranker | 重排序 |
| Reasoning / Thinking / R1-style | 长链推理 |
| Hybrid Thinking | 混合思考(可开关思考模式)|
| MoE | 混合专家架构 |
| Agent / Tool | 强化工具调用能力 |

**上下文长度**
| 标签 | 含义 |
|------|------|
| 8K / 32K / 128K | 已经是基础水平 |
| 256K / 1M / 10M | 2026 年长上下文常态(Gemini 3、Qwen3-Long、MiniMax M)|

**量化(下一章详讲)**
| 标签 | 含义 |
|------|------|
| FP16 / BF16 | 半精度,原始 |
| FP8 | 8 位浮点,H100 / RTX 50 系原生支持 |
| FP4 / NVFP4 / MXFP4 | 4 位浮点,Blackwell(B200)原生支持,2026 新主流 |
| GPTQ / AWQ | GPU 量化(老牌但仍常用) |
| GGUF | CPU/Mac 友好,llama.cpp 生态 |
| MLX | Apple Silicon 专用 |
| EXL2 / EXL3 | ExLlama 格式,GPU 推理速度极快 |
| MNN / ONNX | 端侧部署格式 |

**特殊后缀**
| 标签 | 含义 |
|------|------|
| LoRA / Adapter | 微调"补丁",需配 base 模型一起加载,详见 §9.2.1 |
| GGUF-Q4_K_M | GGUF 4 位量化中等精度 |
| CustomVoice / VoiceClone | TTS 声音克隆版本 |

### 4.3 一个简单的判断顺序

挑模型时按这个顺序读后缀,基本就能判断它适不适合你:

1. **模态** → 看 VL / Omni / Audio,确定输入输出类型
2. **是否对齐** → Base 还是 Instruct/Chat,确定能不能直接聊
3. **专精** → Coder / Math / Reasoner 等,匹配你的任务
4. **参数量** → 7B / 70B,看你的硬件能不能扛
5. **量化格式** → GGUF / AWQ / MLX,看你用什么推理引擎

### 4.4 实战阅读练习

练一练,自己拆解下面这些名字:

1. `Llama-3.1-8B-Instruct`
   → Llama 第 3.1 代 + 8B + 已指令微调,可直接聊天

2. `DeepSeek-R1-Distill-Qwen-32B-AWQ`
   → DeepSeek-R1 蒸馏到 Qwen-32B + AWQ 量化(GPU 推理)

3. `Qwen2.5-VL-7B-Instruct-GGUF`
   → 通义千问 2.5 视觉语言版 + 7B + 指令微调 + GGUF 格式(Mac/CPU 能跑)

4. `bge-reranker-v2-m3`
   → BGE 系列重排序模型 + 第 2 代 + M3 变体

---

## 第 5 章 · 量化是什么

### 5.1 为什么需要量化

模型权重原本用 16 位浮点数(FP16)存。一个 8B 模型 ≈ 16 GB,70B 模型 ≈ 140 GB。
普通人没那么大显存。**量化**就是把每个数字用更少的位数表示,文件变小、显存变少、速度变快,代价是损失一点精度。

类比:把一张 4K 照片压缩成 720p,肉眼看差不多,但文件小很多。

### 5.2 精度档位对照表(2026 年视角)

以 7B 模型为例(原始 FP16 ≈ 14 GB):

| 精度 | 每个参数占用 | 7B 体积 | 质量损失 | 适用场景 |
|------|------------|--------|---------|---------|
| FP32 | 32 位(4 字节) | 28 GB | 基准 | 训练 |
| FP16 / BF16 | 16 位(2 字节) | 14 GB | 几乎无 | 高端推理 |
| FP8 | 8 位浮点 | 7 GB | 极小 | H100/H200/RTX 50 系 |
| INT8 | 8 位整数 | 7 GB | 小 | 通用 |
| **FP4 / NVFP4 / MXFP4** | **4 位浮点** | **3.5 GB** | **小(2026 新主流)** | **B200 / RTX 50 / 5090 原生** |
| INT4 / Q4 | 4 位整数 | 3.5 GB | 中等 | 消费级显卡、Mac |
| Q3 / Q2 | 3 位 / 2 位 | 更小 | 较大 | 极限场景 |
| 1.58-bit (BitNet) | 三值 {-1,0,+1} | 极小 | 实验性 | 端侧 |

**2026 年的关键变化**:
- **FP4 成为新硬件主流**:NVIDIA Blackwell(B200、GB200、RTX 5090)原生支持 FP4 推理,精度接近 FP8 但显存减半,数据中心新部署普遍用它。
- **GGUF 也加入 IQ 系列**:`IQ4_XS`、`IQ3_M` 这种"重要性量化",同位数下精度比传统 Q 系列更好。
- **超低位模型可用**:BitNet b1.58 等"原生 1-bit 训练"模型出现,虽然还没大规模商用,但端侧很有前景。

**经验规律**:
- 4 位以上,日常用基本感觉不到差别
- 3 位以下,会明显变笨
- 同等位数下,新算法(AWQ、IQ、NVFP4)比老算法精度更好

### 5.3 主流量化格式详解

**GGUF(最重要,新手必学)**
- 推理框架:llama.cpp、Ollama、LM Studio
- 平台:CPU、GPU 混合、Mac M 系列、Windows、Linux 都能跑
- 文件:**单文件**,方便分发(一个 .gguf 文件就够了)
- 命名:`Q4_K_M`、`Q5_K_S`、`Q8_0` 等
  - 数字:位数(4=4 位,8=8 位)
  - K_S / K_M / K_L:Small / Medium / Large,精度档位
  - 推荐档位:**Q4_K_M**(平衡)、**Q5_K_M**(质量更好)、**Q8_0**(几乎无损)
- 适合:个人电脑、Mac、没有强 GPU 的环境

**AWQ(GPU 推荐)**
- 全称:Activation-aware Weight Quantization
- 推理框架:vLLM、SGLang、AutoAWQ、TGI
- 特点:激活感知,4 位下精度比 GPTQ 好
- 适合:NVIDIA GPU,生产部署

**GPTQ(老牌 GPU 量化)**
- 推理框架:AutoGPTQ、ExLlama、vLLM
- 早期主流,现在被 AWQ 慢慢取代,但兼容性广
- 适合:NVIDIA GPU

**FP8**
- 不是后训练量化,而是原生 8 位浮点训练/推理
- 需要 H100、H200、RTX 50 系等支持 FP8 的硬件
- 精度几乎无损,数据中心 2024-2025 主流
- `Qwen3-Coder-Next-FP8` 就是这种

**FP4 / NVFP4 / MXFP4(2026 新主流)**
- 4 位浮点(不是整数),硬件原生支持
- NVIDIA Blackwell GPU(B200、GB200、RTX 5090)原生算
- 速度比 FP8 快一倍,显存减半,精度损失很小
- 数据中心 2026 年的新部署默认选项

**MLX**
- Apple 自家深度学习框架的格式
- 专为 M1/M2/M3/M4 芯片优化
- 在 Mac 上比 GGUF 还快一点
- 工具:mlx-lm、LM Studio(支持 MLX 后端)

**EXL2 / EXL3**
- ExLlama / ExLlamaV2 格式
- GPU 速度极快,可指定每层不同位宽(混合精度)
- 适合:有 NVIDIA 显卡的玩家,追求速度

**MNN**
- 阿里巴巴的端侧推理框架
- 主打手机、嵌入式设备
- ModelScope 上有专门的 MNN 模型仓库

**ONNX**
- 跨框架通用格式(ONNX = Open Neural Network Exchange)
- 适合部署到 TensorRT、OpenVINO、Web、移动端
- 大部分模型都能转成 ONNX

**TensorRT-LLM / vLLM 专用包**
- NVIDIA 数据中心高性能推理
- 通常需要在自己机器上"编译",不直接下载

### 5.4 我该选哪种格式?

按硬件选:

| 你的硬件 | 推荐格式 | 推荐工具 |
|---------|---------|---------|
| Mac M1/M2/M3/M4 | GGUF 或 MLX | Ollama、LM Studio |
| Windows + N 卡(8-24G) | GGUF 或 AWQ | LM Studio、Ollama、vLLM |
| Linux + 多张 N 卡 | AWQ / FP8 / GPTQ | vLLM、SGLang |
| Windows / Linux 没显卡 | GGUF(纯 CPU) | Ollama、LM Studio |
| Android / iOS / IoT | MNN / ONNX | MNN、TensorFlow Lite |

按目的选:

- **就想体验** → GGUF + Ollama,最省心
- **想做 RAG / 应用开发** → GGUF + Ollama,或者直接用云 API
- **公司部署给多人用** → AWQ + vLLM(并发高)
- **超大模型(70B+)有限显存** → 选低位 GGUF(Q3、Q4)+ CPU 卸载

### 5.5 一个常见误区

**不是位数越低越好。** 看到 `Q2_K` 和 `Q4_K_M`,别只看体积。Q2 在 7B 这种小模型上几乎不可用,在 70B 这种大模型上才勉强。位数越低,对模型规模要求越高。

经验法则:
- 7B / 8B 模型:不要低于 Q4
- 13B / 14B:Q4 够用
- 32B / 34B:Q4 / Q3 都可
- 70B+:Q3 甚至 Q2 都能用

---

## 第 6 章 · 推理工具生态

模型权重只是文件,得有"推理引擎"加载它才能用。下面按场景介绍主流工具。

### 6.1 个人本地玩(零基础首选)

**Ollama**(命令行,极简)
- 官网:ollama.com
- 跨平台:Windows / Mac / Linux
- 一行命令拉模型:`ollama run llama3.1`
- 自动管理量化版本,默认 GGUF Q4
- 提供 OpenAI 兼容 API(http://localhost:11434)
- 适合:开发者、想用脚本接入应用的人

**LM Studio**(图形界面,小白友好)
- 官网:lmstudio.ai
- 跨平台,带 GUI,内置模型搜索和下载
- 支持 GGUF、MLX、多种量化
- 也能开本地 API 服务
- 适合:零代码用户、Mac 用户

**Jan**(开源版 LM Studio)
- 官网:jan.ai
- 全开源,本地优先,隐私好

**GPT4All**
- 老牌本地 LLM 应用,简单易用

### 6.2 进阶 / 服务器部署

**llama.cpp**(GGUF 的"亲妈")
- GitHub:ggerganov/llama.cpp
- C++ 实现,极致优化,Ollama 和 LM Studio 都基于它
- 支持 CPU、CUDA、Metal、Vulkan、OpenCL 等
- 直接命令行用比较硬核,但性能最好

**vLLM**(GPU 高并发推理首选)
- GitHub:vllm-project/vllm
- 专为生产设计,PagedAttention 技术让吞吐极高
- 支持 AWQ、GPTQ、FP8、原始 BF16
- 提供 OpenAI 兼容 API
- 适合:多人同时用、做 SaaS

**SGLang**
- 类似 vLLM,部分场景更快
- 国产 DeepSeek 等模型常推荐用 SGLang

**TGI(Text Generation Inference)**
- Hugging Face 出品的生产级推理服务

**TensorRT-LLM**
- NVIDIA 官方,极致性能,但部署门槛高

### 6.3 专项工具

**ComfyUI / Automatic1111 / Forge**
- 图像生成(Stable Diffusion / FLUX)的主流 UI
- ComfyUI 是节点式,上限高;A1111 / Forge 更易用

**Whisper.cpp / Faster-Whisper**
- 语音识别本地推理

**Open WebUI**
- 类似 ChatGPT 的 Web 界面,接 Ollama / vLLM 等后端

### 6.4 选型决策树

```
是个人玩?
├── 是 → Mac?
│         ├── 是 → LM Studio (GUI) 或 Ollama (命令行)
│         └── 否 → 有 N 卡?
│                   ├── 是 → LM Studio / Ollama
│                   └── 否 → Ollama 纯 CPU 跑小模型
└── 否(服务器/多人)→ 有 GPU?
                      ├── 是 → vLLM / SGLang
                      └── 否 → llama.cpp server 模式

要做 RAG / Agent 应用?
→ 后端用 Ollama/vLLM,前端用 Open WebUI 或自己写

要训练 / 微调?
→ Hugging Face Transformers + PEFT(LoRA)
→ 或 Unsloth(训练加速,显存友好)
→ 或 LLaMA-Factory(图形化训练工具)
```

---

## 第 7 章 · 实战一:在自己电脑上跑第一个模型

最快路径:**Ollama**。下面是从零到聊天的完整步骤。

### 7.1 安装 Ollama

- Windows / Mac:去 ollama.com 下载安装包,双击装
- Linux:`curl -fsSL https://ollama.com/install.sh | sh`

装完打开终端,输入 `ollama` 看到帮助信息就 OK。

### 7.2 下载并运行模型

挑一个适合你硬件的模型。**2026 年第一次推荐 Qwen3 4B 或 Gemma 3-4B**,小、快、中文也不错,且都带原生多模态(看图)。

```bash
# 拉 Qwen3 4B 并直接进入对话(默认带 thinking 模式)
ollama run qwen3:4b

# Gemma 3 4B(Google,多模态轻量)
ollama run gemma3:4b

# 想要中文更强且能看图,Qwen3-VL 8B
ollama run qwen3-vl:8b
```

第一次会下载几 GB,之后就秒进。退出对话:输入 `/bye` 或按 Ctrl+D。

### 7.3 常用命令

```bash
ollama list              # 看本地装了哪些模型
ollama pull qwen3:8b     # 只下载不进入对话
ollama rm qwen3:4b       # 删除某个模型
ollama ps                # 看正在运行的模型
ollama show qwen3:8b     # 看模型详情
```

### 7.4 用图形界面更舒服:Open WebUI / LM Studio

Ollama 默认在 `http://localhost:11434` 提供 API。配 Open WebUI 就有 ChatGPT 同款界面,支持联网搜索、RAG、Agent 工具调用。

最简单是用 Docker:
```bash
docker run -d -p 3000:8080 --add-host=host.docker.internal:host-gateway \
  -v open-webui:/app/backend/data --name open-webui \
  --restart always ghcr.io/open-webui/open-webui:main
```

打开 `http://localhost:3000`,注册一个本地账号,就能聊了。

不想用 Docker,**LM Studio** 自带聊天界面,2025 年加了 MCP 支持(直接接外部工具),GUI 用户更推荐。

### 7.5 换个模型试试(2026 年推荐)

Ollama 模型库:ollama.com/library

按硬件选:
- **4 GB 显存 / 8 GB 内存(超轻量)**:`qwen3:1.7b`、`gemma3:1b`、`llama3.2:1b`
- **8 GB 显存 / 16 GB 内存**:`qwen3:4b`、`gemma3:4b`、`phi4-mini`
- **16 GB 显存**:`qwen3:14b`、`gemma3:12b`、`mistral:7b`、`deepseek-r1-distill:14b`
- **24 GB 显存(3090/4090/5080)**:`qwen3:30b-a3b`(MoE 真香)、`qwen3:32b`、`gemma3:27b`
- **48 GB+(双卡 / 5090 / 工作站)**:`qwen3:235b-a22b`(Q4)、`llama4:70b`、`deepseek-v3:Q4`
- **想要推理能力**:`deepseek-r1`、`qwen3-thinking`、`qwq`
- **想要看图**:`qwen3-vl`、`gemma3`(原生多模态)、`llava`、`minicpm-v`
- **代码场景**:`qwen3-coder`、`deepseek-coder-v2`、`codestral`

> 2024 年很流行的 `llama3.1:8b`、`qwen2.5:7b` 现在仍能用,但同尺寸下已被新一代取代,新项目首选 Qwen3 / Gemma 3 / Llama 4 系列。

### 7.6 常见问题排查

**模型答得很慢**
→ 显存不够,模型被卸载到 CPU 跑了。换更小或更低量化的版本。

**输出乱码 / 重复**
→ 模型选得不对,或者量化太低。换 Q5 或 Q8 试试。

**显示中文不好**
→ 优先选 Qwen、DeepSeek、GLM、Yi 这些国产系列。

**Mac 跑得慢**
→ 关掉浏览器和后台占内存的应用。M 系列要保证统一内存够用。

---

## 第 8 章 · 实战二:用 API 接入应用

让模型从"聊天玩具"变成"应用零件",关键是 API。

### 8.1 OpenAI 兼容 API:事实标准

几乎所有现代推理引擎(OpenAI、Ollama、vLLM、LM Studio、DeepSeek、阿里百炼、智谱)都遵循同一套接口规范。
学会一套,处处通用。

核心接口:`POST /v1/chat/completions`

### 8.2 Python 调用本地 Ollama

先装库:
```bash
pip install openai
```

写代码:
```python
from openai import OpenAI

# 指向本地 Ollama
client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama"  # 本地随便填
)

resp = client.chat.completions.create(
    model="qwen2.5:7b",
    messages=[
        {"role": "system", "content": "你是一个简洁的助手。"},
        {"role": "user", "content": "用一句话解释什么是量化"}
    ]
)
print(resp.choices[0].message.content)
```

把 `base_url` 改成 OpenAI / DeepSeek / 通义的地址,代码不用改。

### 8.3 流式输出(打字机效果)

```python
stream = client.chat.completions.create(
    model="qwen2.5:7b",
    messages=[{"role": "user", "content": "写一首关于春天的短诗"}],
    stream=True
)
for chunk in stream:
    delta = chunk.choices[0].delta.content
    if delta:
        print(delta, end="", flush=True)
```

### 8.4 多轮对话

API 是无状态的,得自己维护历史:
```python
history = [{"role": "system", "content": "你是助手。"}]
while True:
    user = input("你: ")
    if not user: break
    history.append({"role": "user", "content": user})
    resp = client.chat.completions.create(
        model="qwen2.5:7b", messages=history
    )
    answer = resp.choices[0].message.content
    print("AI:", answer)
    history.append({"role": "assistant", "content": answer})
```

### 8.5 工具调用 / Function Calling

让模型决定"该调用哪个函数",是 Agent 的基础。
```python
tools = [{
    "type": "function",
    "function": {
        "name": "get_weather",
        "description": "查询某城市天气",
        "parameters": {
            "type": "object",
            "properties": {"city": {"type": "string"}},
            "required": ["city"]
        }
    }
}]

resp = client.chat.completions.create(
    model="qwen2.5:7b",
    messages=[{"role": "user", "content": "上海天气怎么样"}],
    tools=tools
)
print(resp.choices[0].message.tool_calls)
```

模型会返回它想调用 `get_weather(city="上海")`,你的代码真正去查天气,再把结果喂回给它,它再生成最终回答。

### 8.6 选 API 服务的建议(2026 年视角)

**国外**
- **OpenAI**:GPT-5、o3/o4 推理模型,综合最强,贵
- **Anthropic Claude**:Claude 4 Opus / Sonnet,写作、长文档、Agent 工具调用强,Code 场景首选
- **Google Gemini**:Gemini 3,多模态、原生 1M-10M 超长上下文、免费额度大
- **xAI Grok**:Grok 4 系列,带实时 X 信息

**国内**
- **DeepSeek**:V3.2 / R1.x,**性价比之王**,推理便宜到离谱
- **阿里百炼(Qwen)**:Qwen3 全家桶,中文好,免费额度大
- **字节豆包(Doubao)**:豆包 1.5,价格极低,推理速度快
- **月之暗面 Kimi**:Kimi K2,长文档处理强
- **智谱 GLM**:GLM-4.6 / Z1,综合能力均衡
- **MiniMax**:海螺、长上下文方向

**自部署 vs 用云 API**
- 个人玩 / 隐私敏感 → 自部署
- 商用 / 高并发 / 不想运维 → 云 API
- 混合策略(2026 年常见):简单任务本地小模型,复杂任务调云端旗舰
- **Prompt Caching**(2025 年起几乎所有大厂都支持):重复的系统提示和上下文可以缓存,价格降 50-90%,长 RAG 场景必用

---

## 第 9 章 · 进阶玩法

### 9.1 RAG(检索增强生成)

让模型回答你**自己的资料**(公司文档、笔记、PDF)。

原理:
```
用户提问
   ↓
[Embedding 模型] 把问题向量化
   ↓
[向量数据库] 找出最相关的几段文档
   ↓
把这几段 + 问题一起喂给 LLM
   ↓
LLM 基于资料回答
```

零代码工具:
- **AnythingLLM**(开源、桌面版,接 Ollama)
- **Dify**(开源、Web 平台)
- **MaxKB / FastGPT / RagFlow**

要写代码:**LangChain** 或 **LlamaIndex** 框架,加一个向量库(Chroma / Milvus / Qdrant)。

关键组件:
- Embedding 模型:bge-m3、text-embedding-3
- 向量库:Chroma(本地易用)、Qdrant、Milvus
- Reranker:bge-reranker-v2-m3(显著提升精度)
- LLM:Qwen2.5、Llama 3.1 等

### 9.2 微调(让模型懂你的领域)

什么时候要微调:
- 提示词调不好的特定输出格式
- 某个垂直领域(法律、医疗)需要稳定专业表达
- 想做风格模仿(客服话术、个人写作风格)

主流方法:
- **LoRA / QLoRA**:消费级显卡能做,小数据集就够(详见 9.2.1)
- **全参数微调**:效果最好,但要多卡

零基础推荐工具:
- **LLaMA-Factory**:图形化界面,支持各大模型
- **Unsloth**:训练加速 2-5 倍,显存省一半
- **Axolotl**:配置文件驱动,工业界爱用

最小数据量经验:几百条高质量样本就有效果,几千条很稳。

#### 9.2.1 LoRA / QLoRA 深入讲解

LoRA / QLoRA 是当前个人玩家**最重要的微调技术**,理解它能省下大量算力和时间。

**为什么需要 LoRA**

传统全量微调要更新模型全部参数,带来三个问题:

1. **显存爆炸**:7B 模型全量微调要 80GB+ 显存(权重 + 梯度 + Adam 优化器状态),普通人玩不起。
2. **存储灾难**:每微调一个版本就是一份完整权重(14GB+),做 10 个领域专家就要 140GB。
3. **过拟合**:小数据集去更新所有参数,容易"学坏"。

核心洞察:微调时权重的变化其实是"低秩"的——可以用一个很小的矩阵来近似表达。既然变化很小,何必动整个模型?

**LoRA 的核心思想**

LoRA = **Lo**w-**R**ank **A**daptation(低秩适配),2021 年微软提出。
一句话原理:**冻结原模型,在旁边加一个小补丁,只训练这个补丁**。

数学直觉:原模型某层权重是矩阵 W(假设 4096×4096,1600 万个数)。
全量微调直接改 W 变成 W + ΔW,LoRA 的发现是这个 ΔW 可以拆成两个瘦长矩阵的乘积:

```
ΔW (4096×4096) ≈ A (4096×8) × B (8×4096)
   1600 万个参数         总共 6.5 万个参数
```

这里的 `8` 叫 **rank(秩)**,通常取 4 / 8 / 16 / 32 / 64。
参数量降了约 245 倍,但效果接近全量微调。

推理时:`y = W·x + (A·B)·x`,左边是原模型,右边是 LoRA 补丁。训练完可以挂着用,也可以合并到 W 里变成新模型。

**QLoRA 又是什么**

QLoRA = **Q**uantized **LoRA**(量化 + LoRA),2023 年华盛顿大学提出。
LoRA 虽然只训练补丁,但**原模型还得加载到显存里参与前向计算**。7B 模型 FP16 加载要 14GB,70B 模型基本免谈。

QLoRA 的做法:把原模型量化到 **4-bit**(NF4 格式)只读、不训练;LoRA 补丁仍然 FP16 训练;计算时临时反量化。

**效果对比**(以 7B 模型微调为例):

| 方法 | 显存需求 | 训练速度 | 效果 |
|------|---------|---------|------|
| 全量微调 | 80 GB+ | 基准 | 最好 |
| LoRA | 20 GB | 略快 | 接近全量 |
| QLoRA | **6-8 GB** | 略慢 | 接近 LoRA |

结论:QLoRA 让 24GB 显卡能微调 33B 模型,48GB 能微调 70B,普通人在家就能玩。

**关键超参数(实操要点)**

- **rank (r)**:LoRA 补丁宽度。常用 8 / 16 / 32 / 64。简单任务 8-16,复杂任务 32-64。
- **alpha**:缩放系数,经验法则 `alpha = 2 × r`。
- **target_modules**:给哪些层挂补丁。推荐全部线性层 `q,k,v,o,gate,up,down`,效果最好。
- **learning_rate**:1e-4 到 3e-4,比全量微调高一个数量级。
- **dropout**:0.05-0.1 防过拟合。

**实战:用 Unsloth 微调一个 7B 模型**

最简单的工具是 Unsloth,2-5 倍训练加速,显存还省一半。

```bash
pip install unsloth
```

最小可运行代码:

```python
from unsloth import FastLanguageModel
from datasets import load_dataset
from trl import SFTTrainer
from transformers import TrainingArguments

# 1. 加载 4-bit 量化的基础模型(QLoRA)
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name="unsloth/Qwen2.5-7B-Instruct",
    max_seq_length=2048,
    load_in_4bit=True,
)

# 2. 给模型装上 LoRA 补丁
model = FastLanguageModel.get_peft_model(
    model,
    r=16,                    # rank
    lora_alpha=32,           # alpha = 2r
    target_modules=["q_proj","k_proj","v_proj","o_proj",
                    "gate_proj","up_proj","down_proj"],
    lora_dropout=0.05,
)

# 3. 准备数据(聊天格式 jsonl)
dataset = load_dataset("json", data_files="my_data.jsonl", split="train")

# 4. 训练
trainer = SFTTrainer(
    model=model, tokenizer=tokenizer, train_dataset=dataset,
    args=TrainingArguments(
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        num_train_epochs=3,
        learning_rate=2e-4,
        output_dir="my_lora",
    ),
)
trainer.train()

# 5. 保存(只保存 LoRA 补丁,几十 MB)
model.save_pretrained("my_lora_adapter")
```

数据格式示例:
```json
{"messages": [
  {"role": "user", "content": "公司退货政策是什么?"},
  {"role": "assistant", "content": "我们支持 30 天无理由退货……"}
]}
```

几百到几千条样本就能训出有效果的领域微调。

**什么时候该用 LoRA / QLoRA**

适合:
- 让模型懂某个垂直领域(法律、医疗、客服话术)
- 模仿特定风格(品牌语气、个人写作风格)
- 让模型稳定输出特定格式(JSON、表格)
- 教会模型新的任务模式

不适合:
- 注入大量新知识(LoRA 学知识效果一般,RAG 更合适)
- 需要根本改变模型行为(可能要全量微调或继续预训练)
- 数据量极大(几百万条以上,LoRA 容量可能不够)

**和其他名词的关系**

| 术语 | 是什么 |
|------|-------|
| PEFT | Parameter-Efficient Fine-Tuning,参数高效微调的总称,LoRA 是其中一种 |
| Adapter | 早期参数高效方法,LoRA 算改进版 |
| Prefix / Prompt Tuning | 另一类 PEFT,在输入端加可训练 token,效果一般不如 LoRA |
| DoRA | LoRA 改进版,2024 年提出,精度略好 |
| LoftQ | QLoRA 的改进,初始化更精准 |
| Hugging Face PEFT 库 | 实现 LoRA / QLoRA 等方法的官方库 |

**和"格式"维度的关系**

LoRA 训练完得到一个 **adapter**(几十 MB ~ 几 GB),它**不是完整模型**,使用时有两条路:

1. **挂着用**:基础模型 + LoRA adapter 一起加载
   - Hugging Face 上 `xxx-lora` 后缀的就是
   - 推理框架支持:vLLM、SGLang、Hugging Face Transformers

2. **合并 + 重新打包**:把 LoRA 合并进基础模型,导出成 GGUF / AWQ
   - 合并后变成独立模型,部署到 Ollama 等

完整链路:
```
Base 模型 → SFT → Instruct
                    ↓
                  LoRA 微调(你的领域数据)
                    ↓
              合并 + 量化打包(GGUF Q4_K_M)
                    ↓
                Ollama 部署
```

这就是个人玩家"训练 + 部署"的标准工作流。

### 9.3 Agent(让模型自己干活)

Agent = LLM + 工具 + 循环。模型自己思考下一步、调用工具、看结果、再思考……直到完成任务。
**2026 年 Agent 已经不是噱头,而是日常**——Claude Code、Cursor Composer、Devin、Manus 这些产品就是 Agent 的成熟形态。

**主流编排框架**
- **LangGraph**(LangChain 升级版):图状态机,生产首选
- **CrewAI / AutoGen / MetaGPT**:多 Agent 协作
- **Dify / Coze / n8n**:零代码 / 低代码搭 Agent
- **Pydantic AI / LlamaIndex Workflows**:类型安全的轻量框架
- **Anthropic Claude Agent SDK / OpenAI Agents SDK**:官方框架,直接对接自家模型

**Agent 模型选择(2026)**
工具调用能力最强的开源:Qwen3、DeepSeek V3.2、GLM-4.6、Llama 4。
闭源:Claude 4 Sonnet/Opus(代码 Agent 之王)、GPT-5、Gemini 3。

**Computer Use / Browser Use(2025-2026 大趋势)**
让 AI 自己操作浏览器、点击屏幕、填表单。
- Anthropic Computer Use(Claude 自带)
- OpenAI Operator
- 开源:Browser Use、Skyvern、Anthropic Computer Use Demo

### 9.4 MCP(Model Context Protocol)

Anthropic 在 2024 年底提出的"工具接入标准协议"。**2026 年已成事实标准**。
一个 MCP server 写好,所有支持 MCP 的客户端(Claude Desktop、Cursor、Cline、Windsurf、VS Code Copilot、Zed、Continue、Open WebUI、ChatGPT 桌面)都能用。

**理解为"AI 界的 USB"**:一次开发,处处插上即用。

**常见 MCP server**
- 官方:filesystem、github、gitlab、slack、postgres、memory、puppeteer
- 社区:notion、figma、linear、jira、google-drive、brave-search、obsidian
- **MCP Marketplace** 上有上千个现成的可以装

**写一个 MCP server 不难**:用 TypeScript / Python SDK 几十行代码就能暴露你的 API、数据库、本地工具给所有 AI 客户端用。

### 9.5 多模态生成实战(2026 版)

**图像生成**
- **FLUX.2**(开源最强,黑森林实验室):ComfyUI 主流选择
- **Stable Diffusion 4 / SDXL Lightning**:老牌生态完善
- **Imagen 4 / Seedream / Midjourney v7**:闭源旗舰
- **GPT Image / Qwen-Image**:对话式生图,改图特别强
- 工具:**ComfyUI**(节点式,上限高)、**Forge / Fooocus**(易用)

**图像编辑**(2025 年新方向)
- **FLUX Kontext**:开源,精准编辑
- **Qwen-Image-Edit**、**GPT Image Edit**、**Nano Banana**(谷歌)

**视频生成**
- **Veo 3**(谷歌,业界天花板)、**Sora 2**(OpenAI)
- **可灵 2.0 / 海螺 02**(国产闭源)
- **Wan 2.5 / 混元 Video**(开源)
- **HeyGen / Runway Gen-4**:数字人和创意视频

**语音 / 音乐**
- 实时语音对话:**GPT-5 Realtime API**、**Gemini Live**、**Doubao Realtime**
- 高质量 TTS / 克隆:**CosyVoice 2**、**F5-TTS**、**ElevenLabs v3**
- 音乐生成:**Suno v5**、**Udio**、**可灵音乐**

**3D / 数字人**
- **Hunyuan3D**(腾讯)、**TripoSR**:文生 3D 模型
- **HeyGen / Synthesia**:数字人视频

---

## 第 10 章 · 学习路径与资源

### 10.1 推荐学习顺序

第 1 周:**会用**
- 装 Ollama,跑一个 7B 模型
- 用 LM Studio / Open WebUI 体验 GUI
- 学会写好提示词(Prompt Engineering)

第 2-3 周:**会接**
- Python + OpenAI 兼容 API
- 流式、多轮、工具调用
- 部署 Open WebUI 给家人朋友用

第 4-6 周:**会做应用**
- RAG:用 AnythingLLM 或 Dify 搭一个知识库
- Agent:用 Dify / Coze 做一个工作流
- 学一点向量数据库

第 2-3 个月:**会调优**
- LoRA 微调一个垂直领域模型
- 学 vLLM 部署生产环境
- 理解推理参数(temperature、top_p、repetition_penalty)

第 4 个月以后:**深入**
- Transformer 原理(看 3Blue1Brown、Andrej Karpathy 视频)
- 论文阅读:从 GPT、LLaMA、DeepSeek 技术报告开始
- 自己跑预训练 / 强化学习实验

### 10.2 推荐资源(2026 年版)

**模型站**
- Hugging Face(全球最大)
- ModelScope 魔搭(阿里,国内快,镜像同步 HF)
- Ollama Library(开箱即用,适合本地)
- HF-Mirror(国内镜像,免翻墙下 HF)

**社区 / 资讯**
- r/LocalLLaMA(reddit,本地 LLM 圣地)
- Hugging Face Daily Papers(每日论文)
- X(Twitter)上的 AI 圈、即刻、知乎
- 公众号:机器之心、量子位、HuggingFace 中文站

**视频教程**
- 3Blue1Brown 神经网络系列(可视化神作)
- Andrej Karpathy "Neural Networks: Zero to Hero" + "Let's reproduce GPT-2"(必看)
- 李沐《动手学深度学习》、《大模型论文精读》系列
- StatQuest、Sebastian Raschka 教程

**实操项目**
- llama.cpp、Ollama、vLLM 官方文档
- LangChain / LlamaIndex / Pydantic AI 教程
- Hugging Face Course(免费,2025 年更新)
- Anthropic Cookbook、OpenAI Cookbook、Google AI Cookbook

**重磅论文(2024-2026,值得读)**
- DeepSeek-V3 / R1 技术报告
- Qwen3 / Llama 4 技术报告
- "Scaling Test-Time Compute"(o1 思路源头)
- GRPO、RLVR 相关论文

### 10.3 心态建议

- 这个领域**每月都有新东西**,不可能学完。学方法、学评估、学迁移。
- 别一上来啃论文。先跑起来,有了直觉再深入。
- 多实验,少争论。同一个问题不同模型表现差很多,自己测最准。
- 不要迷信 benchmark 排行榜。你的真实场景才是 benchmark。
- **2026 年的现实**:旗舰模型已经接近"日常工作够用",别再纠结"哪个最强",纠结"哪个最适合我的场景"。

---

## 附录 · 术语速查表

| 术语 | 全称 / 解释 |
|------|------------|
| LLM | Large Language Model,大语言模型 |
| VLM | Vision Language Model,视觉语言模型 |
| Token | 模型处理的最小单位,1 个汉字约 1-2 token |
| Context / Context Window | 上下文长度,模型一次能"看到"多少 token |
| Parameter | 参数,模型内部的"旋钮" |
| Pretraining | 预训练 |
| SFT | Supervised Fine-Tuning,监督微调 |
| RLHF | Reinforcement Learning from Human Feedback |
| DPO | Direct Preference Optimization,直接偏好优化 |
| LoRA | Low-Rank Adaptation,低秩适配,轻量微调,详见 §9.2.1 |
| QLoRA | 量化 + LoRA,消费级显卡能微调大模型 |
| PEFT | Parameter-Efficient Fine-Tuning,参数高效微调总称 |
| Adapter | LoRA 训练得到的"补丁",需配基础模型加载 |
| MoE | Mixture of Experts,混合专家 |
| Distill | 蒸馏,小模型学大模型 |
| Quantization | 量化 |
| GGUF | llama.cpp 的模型格式 |
| AWQ | Activation-aware Weight Quantization |
| GPTQ | 一种 GPU 量化算法 |
| FP16 / BF16 / FP8 | 不同精度的浮点 |
| FP4 / NVFP4 / MXFP4 | 4 位浮点,Blackwell 原生支持(2026 主流) |
| Embedding | 把内容转成向量 |
| Reranker | 重排序模型 |
| RAG | Retrieval-Augmented Generation,检索增强生成 |
| Agent | 能自主调用工具完成任务的 AI |
| Function Calling / Tool Use | 工具调用 |
| MCP | Model Context Protocol,2026 年事实标准 |
| Computer Use | AI 直接操作屏幕 / 浏览器 |
| Inference | 推理(模型对外提供服务) |
| Prompt | 提示词 |
| System Prompt | 系统提示词,定义角色和规则 |
| Temperature | 温度,控制随机性,越高越发散 |
| Top-p / Top-k | 采样策略 |
| Streaming | 流式输出,边生成边返回 |
| Chain-of-Thought / CoT | 思维链,让模型逐步推理 |
| Reasoning / Thinking Model | 推理模型(o3、R1、Qwen3-Thinking 等) |
| Hybrid Thinking | 混合思考,模型自己决定要不要思考 |
| Thinking Budget | 思考预算,控制模型最多想多少 token |
| RLVR | 可验证奖励强化学习 |
| GRPO | DeepSeek 提出的 RL 算法 |
| Synthetic Data | 合成训练数据 |
| Prompt Caching | 提示词缓存,2025 年起降本利器 |
| Hallucination | 幻觉,模型一本正经胡说 |
| Benchmark | 测试榜 |
| Hugging Face | 全球最大模型社区 |
| ModelScope | 阿里的模型社区(魔搭) |

---

**结尾的话**

不要追求"学完",追求"开始"。装一个 Ollama,跑一个 Qwen3,问它一个你最关心的问题——这一刻起,你就已经入门了。
其余的全是在路上慢慢补的。祝玩得开心。




